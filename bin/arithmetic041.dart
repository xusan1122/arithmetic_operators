// Create function func with arguments
// the task condition is in the Readme file

double func(int a, int b) {
  int a = 2;
  int b = 4;
  return (((b/2)+a)*((b/2+a))*(b/2));
}

void main() {
  print(func(2,4));
}
